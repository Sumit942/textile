package com.example.textile.repo;

import com.example.textile.entity.ProductExclude;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductExcludeRepository extends JpaRepository<ProductExclude, Long> {
}
